# This configuration file is for pytest.
# The `pythonpath` setting is crucial for ensuring that pytest can correctly
# locate and import our application modules (like `app.main`) from the
# project's root directory. Without this, `import app.main` would fail.

[pytest]
pythonpath = .