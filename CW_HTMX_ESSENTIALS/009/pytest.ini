[pytest]
# This configuration tells pytest to add the project's root directory
# to Python's path. This is crucial for allowing tests in the `tests/`
# directory to correctly import modules from the `app/` directory,
# for example: `from app.main import app`.
pythonpath = .