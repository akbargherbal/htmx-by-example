[pytest]
# This configuration is essential for running tests correctly from the project root.
# It tells pytest to add the current directory (.) to the Python path.
# This allows test files in `tests/` to import modules from `app/`
# using a standard path, e.g., `from app.main import app`.
pythonpath = .