[pytest]
# This configuration is essential for running tests from the project root.
# It tells pytest to add the current directory (.) to Python's path.
# This allows `from app.main import app` in `tests/test_api.py` to work
# correctly by ensuring the 'app' package is discoverable.
pythonpath = .