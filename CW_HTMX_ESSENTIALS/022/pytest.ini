[pytest]
# This configuration is essential for making sure pytest can find and import
# our application modules (like 'app.main') from the project's root directory.
# By setting pythonpath = ., we are telling pytest to add the current directory
# (the project root) to Python's import path.
pythonpath = .