[pytest]
# This configuration is essential for running tests from the project root.
# It tells pytest to add the current directory (.) to the Python path.
# This allows test files in `tests/` to correctly import modules from `app/`,
# for example: `from app.main import app`.
pythonpath = .