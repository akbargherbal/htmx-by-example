[pytest]
# This configuration is essential for running tests from the project root.
# It tells pytest to add the current directory (.) to the Python path.
# Without this, `from app.main import app` in `tests/test_api.py` would fail
# because Python wouldn't know where to find the 'app' module.
pythonpath = .