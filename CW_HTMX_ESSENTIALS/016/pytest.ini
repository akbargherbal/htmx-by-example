[pytest]
# This configuration is essential for running tests correctly from the project root.
# It tells pytest to add the current directory (.) to Python's path.
# This allows `import app.main` to work seamlessly within the `tests/` directory
# without requiring complex relative import statements.
pythonpath = .